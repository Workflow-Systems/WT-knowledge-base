# hosting.json

В файле задается IP-адрес или доменное имя сервера и порт сервера.

Пример содержимого файла:

```json
{
    "server.urls": "http://localhost:50707",
    "server.connectionLimit": 5000,
    "server.threadCount": 3000
}
```
